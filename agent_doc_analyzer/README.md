# Agent Document Analyzer

This project is an AI-assisted software solution designed to autonomously process unstructured financial text files, compute financial totals, and normalize currencies into a single base currency (EUR).

## Structure
- `config/`: Configuration settings.
- `src/`: Core logic (agent, tools, utils).
- `tests/`: Unit testing for the components.
